<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exa Instant Sales Deck</title>
    <style>
        :root {
            --bg-color: #050505;
            --text-color: #ffffff;
            --accent-primary: #a855f7; /* Purple */
            --accent-secondary: #3b82f6; /* Blue */
            --accent-success: #22c55e; /* Green */
            --accent-danger: #ef4444; /* Red */
            --sidebar-width: 250px;
            --font-main: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: var(--font-main);
            overflow: hidden;
            height: 100vh;
            display: flex;
        }

        /* Sidebar Navigation */
        #sidebar {
            width: var(--sidebar-width);
            background-color: #0a0a0a;
            border-right: 1px solid #222;
            padding: 2rem 1rem;
            display: flex;
            flex-direction: column;
            z-index: 100;
        }

        .brand {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--text-color);
            margin-bottom: 3rem;
            letter-spacing: -1px;
        }

        .brand span {
            color: var(--accent-primary);
        }

        .chapter-list {
            list-style: none;
        }

        .chapter-item {
            padding: 0.8rem 1rem;
            margin-bottom: 0.5rem;
            cursor: pointer;
            border-radius: 6px;
            color: #888;
            transition: all 0.3s ease;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .chapter-item:hover {
            background-color: #1a1a1a;
            color: #fff;
        }

        .chapter-item.active {
            background-color: rgba(168, 85, 247, 0.1);
            color: var(--accent-primary);
            border-left: 3px solid var(--accent-primary);
        }

        /* Main Content Area */
        #main-display {
            flex: 1;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* Slide Container */
        .slide {
            position: absolute;
            width: 80%;
            height: 80%;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease, transform 0.6s ease;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            pointer-events: none; /* Prevent interaction when hidden */
        }

        .slide.active {
            opacity: 1;
            transform: translateY(0);
            pointer-events: all;
            z-index: 10;
        }

        /* Typography & Elements */
        h1 {
            font-size: 4rem;
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            letter-spacing: -2px;
            background: linear-gradient(to right, #fff, #ccc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        h2 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 2rem;
        }

        p {
            font-size: 1.5rem;
            color: #aaa;
            max-width: 800px;
            line-height: 1.5;
        }

        .highlight {
            color: var(--accent-primary);
            font-weight: 700;
        }

        .big-number {
            font-size: 8rem;
            font-weight: 900;
            color: var(--accent-primary);
            line-height: 1;
            margin: 1rem 0;
            text-shadow: 0 0 40px rgba(168, 85, 247, 0.3);
        }

        .stat-label {
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #666;
        }

        /* Visualizations */
        .chart-container {
            display: flex;
            align-items: flex-end;
            gap: 2rem;
            height: 300px;
            margin-top: 2rem;
        }

        .bar-group {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1rem;
        }

        .bar {
            width: 60px;
            background-color: #333;
            border-radius: 8px 8px 0 0;
            position: relative;
            transition: height 1s ease-out;
        }

        .bar.exa {
            background: linear-gradient(to top, var(--accent-primary), var(--accent-secondary));
            box-shadow: 0 0 20px rgba(168, 85, 247, 0.4);
        }

        .bar-label {
            font-size: 1rem;
            color: #888;
        }

        .equation {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            font-size: 2.5rem;
            font-weight: 700;
            margin-top: 2rem;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            margin-top: 2rem;
            width: 100%;
            max-width: 900px;
        }

        .feature-card {
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.1);
            padding: 2rem;
            border-radius: 12px;
            text-align: left;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            color: #fff;
            margin-bottom: 0.5rem;
        }

        .feature-card p {
            font-size: 1rem;
            color: #888;
        }

        /* Navigation Controls */
        .controls {
            position: absolute;
            top: 2rem;
            right: 2rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            font-family: monospace;
            color: #666;
            z-index: 20;
        }

        .slide-counter {
            font-size: 1rem;
            border: 1px solid #333;
            padding: 0.4rem 0.8rem;
            border-radius: 4px;
        }

        /* Key Hint Video Styling */
        .key-hint-container {
            position: absolute;
            bottom: 40px;
            right: 40px;
            width: 80px;
            height: 80px;
            opacity: 0.8;
            transition: opacity 0.3s ease;
        }

        .key-hint-container video {
            width: 100%;
            height: 100%;
            object-fit: contain;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.5);
        }
        
        .key-hint-label {
            font-size: 0.8rem;
            color: #666;
            margin-top: 5px;
            text-align: center;
            font-family: monospace;
        }

        /* Animations */
        @keyframes pulse {
            0% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.05); opacity: 1; }
            100% { transform: scale(1); opacity: 0.8; }
        }

        .pulsing-circle {
            width: 150px;
            height: 150px;
            background: radial-gradient(circle, var(--accent-primary) 0%, transparent 70%);
            border-radius: 50%;
            margin-bottom: 2rem;
            animation: pulse 3s infinite ease-in-out;
        }

    </style>
</head>
<body>

    <nav id="sidebar">
        <div class="brand">Exa<span>.ai</span></div>
        <ul class="chapter-list">
            <li class="chapter-item active" onclick="goToChapter(0)">Introduction</li>
            <li class="chapter-item" onclick="goToChapter(1)">The Problem</li>
            <li class="chapter-item" onclick="goToChapter(3)">The Solution</li>
            <li class="chapter-item" onclick="goToChapter(5)">Technology</li>
            <li class="chapter-item" onclick="goToChapter(7)">Business Value</li>
            <li class="chapter-item" onclick="goToChapter(8)">Conclusion</li>
        </ul>
    </nav>

    <main id="main-display">
        <div class="controls">
            <span class="slide-counter" id="slide-counter">1/9</span>
        </div>

        <div class="slide active" data-chapter="0">
            <div class="pulsing-circle"></div>
            <h1>Exa Instant</h1>
            <p>The Search Engine for <span class="highlight">AI Agents</span>.</p>
            
            <div class="key-hint-container">
                <video autoplay loop muted playsinline>
                    <source src="right arrow Key button.mp4" type="video/mp4">
                    </video>
                <div class="key-hint-label">Next</div>
            </div>
        </div>

        <div class="slide" data-chapter="1">
            <h2 style="color: var(--accent-danger)">Latency Kills Agent Workflows</h2>
            <div class="equation">
                <span>1s Search</span>
                <span>×</span>
                <span>10 Steps</span>
                <span>=</span>
                <span style="color: var(--accent-danger)">10s Lag</span>
            </div>
            <p style="margin-top: 2rem">Traditional search is too slow for real-time reasoning.</p>
        </div>

        <div class="slide" data-chapter="1">
            <h2>The "Wrapper" Bottleneck</h2>
            <div style="display: flex; align-items: center; gap: 1rem; margin: 2rem 0; opacity: 0.7;">
                <div style="border: 1px solid #444; padding: 1rem; border-radius: 8px;">Agent</div>
                <div>→</div>
                <div style="border: 1px solid #444; padding: 1rem; border-radius: 8px; background: #222;">Wrapper API</div>
                <div>→</div>
                <div style="border: 1px solid #444; padding: 1rem; border-radius: 8px;">Legacy Search</div>
                <div>→</div>
                <div style="border: 1px solid #444; padding: 1rem; border-radius: 8px; background: #222;">Wrapper API</div>
                <div>→</div>
                <div style="border: 1px solid #444; padding: 1rem; border-radius: 8px;">Agent</div>
            </div>
            <p><span class="highlight">700ms - 1000ms</span> of wasted overhead.</p>
        </div>

        <div class="slide" data-chapter="2">
            <h2>Enter Exa Instant</h2>
            <div class="big-number">&lt; 200ms</div>
            <p class="stat-label">Total Response Time</p>
            <p style="margin-top: 1rem; color: var(--accent-success)">Network latency as low as 50ms</p>
        </div>

        <div class="slide" data-chapter="2">
            <h2>15x Faster Than Competitors</h2>
            <div class="chart-container">
                <div class="bar-group">
                    <div class="bar exa" style="height: 200px;"></div>
                    <span class="bar-label" style="color: var(--accent-primary); font-weight: bold;">Exa</span>
                </div>
                <div class="bar-group">
                    <div class="bar" style="height: 40px; background: #444;"></div>
                    <span class="bar-label">Tavily</span>
                </div>
                <div class="bar-group">
                    <div class="bar" style="height: 30px; background: #444;"></div>
                    <span class="bar-label">Brave</span>
                </div>
                <div class="bar-group">
                    <div class="bar" style="height: 20px; background: #444;"></div>
                    <span class="bar-label">Google</span>
                </div>
            </div>
            <p style="margin-top: 2rem; font-size: 1rem;">Based on SealQA benchmarks</p>
        </div>

        <div class="slide" data-chapter="3">
            <h2>End-to-End Neural Search</h2>
            <div class="feature-grid">
                <div class="feature-card">
                    <h3>Not a Wrapper</h3>
                    <p>Built on a proprietary neural stack from crawler to inference.</p>
                </div>
                <div class="feature-card">
                    <h3>Semantic Understanding</h3>
                    <p>Uses embeddings to understand intent, not just keyword matching.</p>
                </div>
            </div>
        </div>

        <div class="slide" data-chapter="3">
            <h2>LLM-Ready Data</h2>
            <div style="background: #111; padding: 1.5rem; border-radius: 8px; font-family: monospace; text-align: left; width: 600px; border: 1px solid #333;">
                <span style="color: #888">// Returns clean content, not messy HTML</span><br>
                <span style="color: var(--accent-primary)">const</span> result = <span style="color: var(--accent-secondary)">await</span> exa.search(<br>
                &nbsp;&nbsp;<span style="color: #ce9178">"latest AI news"</span>,<br>
                &nbsp;&nbsp;{ type: <span style="color: #ce9178">"neural"</span> }<br>
                );
            </div>
            <p style="margin-top: 2rem">No scraping scripts required.</p>
        </div>

        <div class="slide" data-chapter="4">
            <h2>Scalable & Efficient</h2>
            <div class="big-number" style="font-size: 6rem;">$5.00</div>
            <p class="stat-label">Per 1,000 Requests</p>
            <p style="margin-top: 2rem">Make search a primitive, not a luxury.</p>
        </div>

        <div class="slide" data-chapter="5">
            <h1>Eliminate the Bottleneck</h1>
            <p style="margin-bottom: 3rem;">Build the next generation of real-time agents today.</p>
            <div style="padding: 1rem 2rem; border: 2px solid var(--accent-primary); border-radius: 50px; color: var(--accent-primary); font-weight: 700; font-size: 1.2rem;">
                dashboard.exa.ai
            </div>
        </div>

    </main>

    <script>
        // State
        let currentSlide = 0;
        const slides = document.querySelectorAll('.slide');
        const totalSlides = slides.length;
        const counterElement = document.getElementById('slide-counter');
        const chapterItems = document.querySelectorAll('.chapter-item');

        // Initial render
        updateUI();

        // Event Listeners
        document.addEventListener('keydown', (e) => {
            if (e.key === 'ArrowRight' || e.key === ' ' || e.key === 'Enter') {
                nextSlide();
            } else if (e.key === 'ArrowLeft') {
                prevSlide();
            }
        });

        // Functions
        function nextSlide() {
            if (currentSlide < totalSlides - 1) {
                currentSlide++;
                updateUI();
            }
        }

        function prevSlide() {
            if (currentSlide > 0) {
                currentSlide--;
                updateUI();
            }
        }

        function goToChapter(slideIndex) {
            currentSlide = slideIndex;
            updateUI();
        }

        function updateUI() {
            // Update Slide Visibility
            slides.forEach((slide, index) => {
                if (index === currentSlide) {
                    slide.classList.add('active');
                } else {
                    slide.classList.remove('active');
                }
            });

            // Update Counter
            counterElement.innerText = `${currentSlide + 1}/${totalSlides}`;

            // Update Chapter Sidebar Highlight
            const activeSlide = slides[currentSlide];
            const currentChapterIndex = activeSlide.getAttribute('data-chapter');
            
            chapterItems.forEach((item, index) => {
                if (index == currentChapterIndex) {
                    item.classList.add('active');
                } else {
                    item.classList.remove('active');
                }
            });
        }
    </script>
</body>
</html>
